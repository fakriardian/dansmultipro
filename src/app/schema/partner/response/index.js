const listPartnerResSchema = require('./listPartnerResSchema');
const partnerAccessResSchema = require('./partnerAccessResSchema');

module.exports = {
    listPartnerResSchema,
    partnerAccessResSchema
};
